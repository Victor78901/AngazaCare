version: "3.9"

services:
  angazacare:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - angazacare_db:/app/angazacare.db
    restart: unless-stopped

volumes:
  angazacare_db:
